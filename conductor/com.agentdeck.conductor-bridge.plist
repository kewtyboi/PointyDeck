<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.agentdeck.conductor-bridge</string>

    <key>ProgramArguments</key>
    <array>
        <string>__PYTHON3__</string>
        <string>__BRIDGE_PATH__</string>
    </array>

    <key>RunAtLoad</key>
    <true/>

    <key>KeepAlive</key>
    <true/>

    <key>StandardOutPath</key>
    <string>__LOG_PATH__</string>

    <key>StandardErrorPath</key>
    <string>__LOG_PATH__</string>

    <key>WorkingDirectory</key>
    <string>__HOME__</string>

    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <string>__HOME__</string>
    </dict>

    <!-- Restart after 10 seconds if it crashes -->
    <key>ThrottleInterval</key>
    <integer>10</integer>

    <!-- Removed LowPriorityIO throttling: IOPOL_THROTTLE delays subprocess
         spawns (50-200ms each) under any disk pressure. The bridge spawns
         agent-deck subprocesses on every message path. -->
    <key>LowPriorityIO</key>
    <false/>

    <!-- Interactive gives the bridge the highest background QoS and prevents
         App Nap from throttling it when no visible window is present. -->
    <key>ProcessType</key>
    <string>Interactive</string>
</dict>
</plist>
